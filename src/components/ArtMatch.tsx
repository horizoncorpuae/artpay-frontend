import { FeedCard } from "../features/artmatch";

const ArtMatch = () => {
  return (
    <section className={"artmatch flex-1 px-8 md:px-0"}>
      <FeedCard />
    </section>
  );
};

export default ArtMatch;